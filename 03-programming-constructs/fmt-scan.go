package main

import "fmt"

func main() {
	/*
		var firstName, lastName string
		// fmt.Println(&name)
		fmt.Scanln(&firstName, &lastName)
		fmt.Printf("Hi %s %s, Have a nice day!\n", firstName, lastName)
	*/

	var no int
	fmt.Scanln(&no)
	fmt.Println(no)

}
